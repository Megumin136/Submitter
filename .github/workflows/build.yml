name: Auto Build & Release on Push

on:
  push:
    branches:
      - master  # 只要 push 到 master 分支就触发

permissions:
  contents: write   # ✅ 关键：允许创建 release
  id-token: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pyinstaller requests

      - name: Build executable
        run: |
          pyinstaller "Dumbass Submitter.py" --onefile --name "DumbassSubmitter"

      # ✅ 重命名产物
      - name: Rename output (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist_output
          if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
            mv dist/DumbassSubmitter dist_output/DumbassSubmitter-linux
          else
            mv dist/DumbassSubmitter dist_output/DumbassSubmitter-macos
          fi
        shell: bash

      - name: Rename output (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          mkdir dist_output
          move dist\DumbassSubmitter.exe dist_output\DumbassSubmitter-windows.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: DumbassSubmitter-${{ matrix.os }}
          path: dist_output/

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write   # ✅ release 任务也显式声明权限

    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release_files

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "自动构建版 - ${{ github.run_number }}"
          tag_name: "build-${{ github.run_number }}"
          body: |
            ✅ 自动构建完成于：${{ github.event.head_commit.timestamp }}
            👤 提交者：${{ github.actor }}
            📦 包含以下平台：
            - Windows (.exe)
            - macOS
            - Linux
          files: release_files/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
